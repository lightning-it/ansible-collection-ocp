---
- name: Skip agent install (test/stub mode)
  ansible.builtin.meta: end_play
  when: ocp_install_agent_skip | bool

- name: Map prefixed variables to legacy names
  ansible.builtin.set_fact:
    ocp_install_delegate_to: >-
      {{
        ocp_install_delegate_to
        | default(ocp_install_agent_delegate_to)
      }}
    ocp_install_dir: >-
      {{
        ocp_install_dir
        | default(ocp_install_agent_dir)
      }}
    ocp_install_platform: >-
      {{
        ocp_install_platform
        | default(ocp_install_agent_platform)
      }}
    ocp_install_kubeconfig_dir: >-
      {{
        ocp_install_kubeconfig_dir
        | default(ocp_install_agent_kubeconfig_dir)
      }}
    ocp_install_vsphere: >-
      {{
        ocp_install_vsphere
        | default(ocp_install_agent_vsphere)
      }}
    ocp_install_vcenter_hostname: >-
      {{
        ocp_install_vcenter_hostname
        | default(ocp_install_agent_vcenter_hostname)
      }}
    ocp_install_vcenter_password: >-
      {{
        ocp_install_vcenter_password
        | default(ocp_install_agent_vcenter_password)
      }}
    ocp_install_vcenter_username: >-
      {{
        ocp_install_vcenter_username
        | default(ocp_install_agent_vcenter_username)
      }}
    ocp_install_vsphere_cluster: >-
      {{
        ocp_install_vsphere_cluster
        | default(ocp_install_agent_vsphere_cluster)
      }}
    ocp_install_vsphere_datacenter: >-
      {{
        ocp_install_vsphere_datacenter
        | default(ocp_install_agent_vsphere_datacenter)
      }}
    ocp_install_vsphere_network: >-
      {{
        ocp_install_vsphere_network
        | default(ocp_install_agent_vsphere_network)
      }}
    ocp_install_base_domain: >-
      {{
        ocp_install_base_domain
        | default(ocp_install_agent_base_domain)
      }}
    ocp_install_cluster_name: >-
      {{
        ocp_install_cluster_name
        | default(ocp_install_agent_cluster_name)
      }}
    ocp_install_vault_path: >-
      {{
        ocp_install_vault_path
        | default(ocp_install_agent_vault_path)
      }}
    ocp_install_version: >-
      {{
        ocp_install_version
        | default(ocp_install_agent_version)
      }}
    ocp_install_os: >-
      {{
        ocp_install_os
        | default(ocp_install_agent_os)
      }}
    ocp_install_arch: >-
      {{
        ocp_install_arch
        | default(ocp_install_agent_arch)
      }}
    ocp_install_loadbalancer_usermanaged: >-
      {{
        ocp_install_loadbalancer_usermanaged
        | default(ocp_install_agent_loadbalancer_usermanaged)
      }}
    ocp_install_mirror: >-
      {{
        ocp_install_mirror
        | default(ocp_install_agent_mirror)
      }}
    ocp_install_occlient: >-
      {{
        ocp_install_occlient
        | default(ocp_install_agent_occlient)
      }}
    ocp_install_download_url: >-
      {{
        ocp_install_download_url
        | default(ocp_install_agent_download_url)
      }}
    ocp_install_machine_network: >-
      {{
        ocp_install_machine_network
        | default(ocp_install_agent_machine_network)
      }}
    ocp_install_service_network: >-
      {{
        ocp_install_service_network
        | default(ocp_install_agent_service_network)
      }}
    ocp_install_pod_network: >-
      {{
        ocp_install_pod_network
        | default(ocp_install_agent_pod_network)
      }}
    ocp_install_proxy: >-
      {{
        ocp_install_proxy
        | default(ocp_install_agent_proxy)
      }}
    ocp_install_proxy_url: >-
      {{
        ocp_install_proxy_url
        | default(ocp_install_agent_proxy_url)
      }}
    ocp_install_no_proxy: >-
      {{
        ocp_install_no_proxy
        | default(ocp_install_agent_no_proxy)
      }}
    ocp_install_pullsecret: >-
      {{
        ocp_install_pullsecret
        | default(ocp_install_agent_pullsecret)
      }}
    ocp_install_registry_quayio: >-
      {{
        ocp_install_registry_quayio
        | default(ocp_install_agent_registry_quayio)
      }}
    ocp_install_ssh_pub_keys: >-
      {{
        ocp_install_ssh_pub_keys
        | default(ocp_install_agent_ssh_pub_keys)
      }}
    ocp_install_registry: >-
      {{
        ocp_install_registry
        | default(ocp_install_agent_registry)
      }}
    ocp_install_root_ca: >-
      {{
        ocp_install_root_ca
        | default(ocp_install_agent_root_ca)
      }}
    ocp_install_ca_o: >-
      {{
        ocp_install_ca_o
        | default(ocp_install_agent_ca_o)
      }}
    ocp_install_additional_trust_bundle: >-
      {{
        ocp_install_additional_trust_bundle
        | default(ocp_install_agent_additional_trust_bundle)
      }}
    ocp_install_api_vip: >-
      {{
        ocp_install_api_vip
        | default(ocp_install_agent_api_vip)
      }}
    ocp_install_apps_vip: >-
      {{
        ocp_install_apps_vip
        | default(ocp_install_agent_apps_vip)
      }}
    ocp_install_ifname: >-
      {{
        ocp_install_ifname
        | default(ocp_install_agent_ifname)
      }}
    ocp_install_gateway: >-
      {{
        ocp_install_gateway
        | default(ocp_install_agent_gateway)
      }}
    ocp_install_dns_servers: >-
      {{
        ocp_install_dns_servers
        | default(ocp_install_agent_dns_servers)
      }}
    ocp_install_netmask_prefix: >-
      {{
        ocp_install_netmask_prefix
        | default(ocp_install_agent_netmask_prefix)
      }}
    ocp_install_image_tag_mirrors: >-
      {{
        ocp_install_image_tag_mirrors
        | default(ocp_install_agent_image_tag_mirrors)
      }}
    ocp_install_taints_ocs: >-
      {{
        ocp_install_taints_ocs
        | default(ocp_install_agent_taints_ocs)
      }}
    ocp_install_odf: >-
      {{
        ocp_install_odf
        | default(ocp_install_agent_odf)
      }}
    ocp_install_engine_mount_point: >-
      {{
        ocp_install_engine_mount_point
        | default(ocp_install_agent_engine_mount_point)
      }}
    ocp_install_hashi_vault_auth_method: >-
      {{
        ocp_install_hashi_vault_auth_method
        | default(ocp_install_agent_hashi_vault_auth_method)
      }}
    ocp_install_cluster_kubeconfig_vault_key: >-
      {{
        ocp_install_cluster_kubeconfig_vault_key
        | default(ocp_install_agent_cluster_kubeconfig_vault_key)
      }}
    ocp_install_hashi_vault_validate_certs: >-
      {{
        ocp_install_hashi_vault_validate_certs
        | default(ocp_install_agent_hashi_vault_validate_certs)
      }}
    ocp_install_cluster_sub_dirs: >-
      {{
        ocp_install_cluster_sub_dirs
        | default(ocp_install_agent_cluster_sub_dirs)
      }}
- name: Ensure all needed vars are present
  ansible.builtin.include_tasks: assert.yml
  tags:
    - always

- name: Fetch values from Vault or fallback
  ansible.builtin.set_fact:
    ocp_install_additional_trust_bundle: >-
      {{
        lookup(
          'community.hashi_vault.hashi_vault',
          g_root_ca_lookup,
          default=ocp_install_additional_trust_bundle
        )
      }}
    ocp_install_root_ca: >-
      {{
        lookup(
          'community.hashi_vault.hashi_vault',
          g_root_ca_lookup,
          default=ocp_install_root_ca
        )
      }}
    ocp_install_pullsecret: >-
      {{
        lookup(
          'community.hashi_vault.hashi_vault',
          ocp_install_pullsecret_lookup,
          default=ocp_install_pullsecret
        )
      }}
  no_log: true
  tags:
    - always

- name: Retrieve current secret from Hashicorp Vault1
  delegate_to: "{{ ocp_install_delegate_to }}"
  run_once: true
  community.hashi_vault.vault_kv2_get:
    url: "{{ vault_address }}"
    engine_mount_point: "{{ ocp_install_engine_mount_point }}"
    path: "{{ ocp_install_vault_path }}/install"
    auth_method: "{{ ocp_install_hashi_vault_auth_method }}"
    role_id: "{{ ansible_hashi_vault_role_id | default('') }}"
    secret_id: "{{ ansible_hashi_vault_secret_id | default('') }}"
    validate_certs: "{{ ocp_install_hashi_vault_validate_certs }}"
  register: current
  no_log: true

- name: Install OCP
  delegate_to: "{{ ocp_install_delegate_to }}"
  run_once: true
  when:
    - current.secret.password is not defined or current.secret.password == ''
  block:
    - name: Prepare OCP install
      ansible.builtin.include_tasks: prepare_ocp4.yml

    - name: Maintain SSH keys
      ansible.builtin.include_tasks: maintain_ssh_keys.yml

    - name: Manage install config
      ansible.builtin.include_tasks: manage_install_config.yml

    - name: Manage agent config
      ansible.builtin.include_tasks: manage_agent_config.yml

    - name: Maintain kube credentials
      ansible.builtin.include_tasks: maintain_kube_creds.yml

    - name: Poweron VMs
      ansible.builtin.include_tasks: power_on_vms.yml

    - name: Wait for install complete agent based
      ansible.builtin.include_tasks: wait_for_install_completed.yml

- name: Post Install
  ansible.builtin.include_tasks: post_ocp.yml
  tags: ocp_install_post

- name: Setup Infra and App Nodes
  ansible.builtin.import_tasks: setup_nodes.yml
  tags: ocp_install_infra
