---
- name: Wait for install complete agent based
  delegate_to: "{{ install_agent_delegate_to }}"
  run_once: true
  ansible.builtin.command:
    cmd: >-
      {{ install_agent_cluster_sub_dirs.download }}/openshift-install
      agent wait-for install-complete
      --dir {{ install_agent_cluster_sub_dirs.config }}
  changed_when: false
  retries: 60
  delay: 60
