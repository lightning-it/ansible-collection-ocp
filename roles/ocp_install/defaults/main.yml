---
# Entrypoint for OCP installation. Dispatches to method-specific roles.
# Supported methods: upi | agent
ocp_install_method: agent

# Common install inputs (shared across methods).
ocp_install_cluster_name: ""
ocp_install_base_domain: ""
ocp_install_pullsecret: ""
ocp_install_ssh_pub_keys: []
