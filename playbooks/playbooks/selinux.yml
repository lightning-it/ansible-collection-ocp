---
# Focused example playbook for an ocp role.
#
# Use this when you want to test or apply an OCP role independently.

- name: Manage OCP operator
  hosts: localhost
  connection: local
  gather_facts: false

  roles:
    - role: lit.ocp.ocp_manage_operator
